import axios from "axios";
import { IHttpClient } from "./IHttpClient";

export default class HttpClient {
    // fetch = <T>(url: string): T => {
    //     let response: T;
    //     axios.get<T>(url)
    //         .then((data) => {
    //             console.log(data);
    //             response = data.data;
    //         });
    //     return response;
    // };
    // fetchById = async <T>(url: string, id: string | number) => {
    //     await axios.get<T>(url)
    //         .then((response) => {
    //             console.log(response);
    //             return response;
    //         });
    // };
}